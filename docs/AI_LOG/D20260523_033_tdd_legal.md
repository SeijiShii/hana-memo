# D20260523_033 — /flow:tdd legal (UI 非依存コア)

```yaml
session_id: D20260523_033_tdd_legal
command: /flow:tdd
mode: feature (UI 非依存コアのみ、React component / hook / Markdown render は defer)
target: legal
started_at: 2026-05-23T18:02:00+09:00
last_updated: 2026-05-23T18:04:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_006_feature_legal, D20260523_032_tdd__shared_ai]
dispatched_by: /flow:auto (continuous loop iteration 5、機能フェーズ開始。spec §4.5.2b に従い pivot 質問なしで継続)
```

---

## Step 1-4: スコープ + 軽重判定

`legal` は同意取得 UI + 静的法務ページ。decouple 方針を機能にも適用し **UI 非依存の同意ドメインロジックのみ実装**:

### 実装 (今回、純ロジック + DI)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `errors.ts` | ConsentError | UT-LE-E03 |
| `versions.ts` | LATEST_VERSIONS + parseSemver + compareVersion + needsReConsent | UT-LE-V01〜V04, H04〜H06 |
| `consent.ts` | validateConsentInput / buildConsentRecords (ip_hash 透過) / deriveLatestConsents / recordConsents (ConsentStore DI) | UT-LE-A01/A02/A04/A05/A06/E03 |
| `index.ts` | barrel | |

- ip_hash は `_shared/helpers/id.ts hashIp` (実装済) を呼び出し側で計算 → buildConsentRecords に渡す (純度維持)。

### Defer (app bootstrap、React 必要)
- `InitialConsent` / `ReConsent` / `LegalPage` React component (UT-LE-I/R/P)
- `useConsentStatus` / `useLatestVersions` hook + localStorage キャッシュ (UT-LE-H01〜H03)
- Markdown レンダリング + XSS sanitize (DOMPurify、UT-LE-P02)
- 実 DB INSERT (Drizzle consent_logs) + retry (UT-LE-A03/E01/E02、ConsentStore 実装)

---

## decisions

### D20260523-097 — Step 1 スコープ (legal UI decouple)

- **chosen_type**: auto-recommended (decouple 方針を機能に適用、spec §4.5.2b で pivot 質問不要)
- **chosen**: versions/consent/errors コアを実装、React component/hook/Markdown/DB glue は app bootstrap へ defer
- **context**: legal は同意 UI + 静的ページ。テスト可能なドメインロジック (semver 再同意判定 / consent record 構築・検証 / latest 導出) を切り出し。ip_hash は実装済 helpers/id.ts hashIp 再利用

### D20260523-098 — Step 6 全テスト結果

- **chosen_type**: auto-recommended
- **chosen**: 22 tests pass、全体 284/284 pass、typecheck clean
- **context**: legal 行 98.86% / 分岐 94.59% (errors/versions 100%、consent 100% line)。`src/features/` ディレクトリ初出 (機能フェーズ開始)

---

## 生成・更新アーティファクト
- 実コード: `src/features/legal/{errors,versions,consent,index}.ts` (4 新規) + test (2 新規、22 tests)
- レポート: `101_legal_IMPL_REPORT.md` / `102_legal_UNIT_TEST_REPORT.md`
- INDEX: `legal/INDEX.md` / `docs/INDEX.md` → コア実装完了
- SCENARIO §5: Phase 3 進行更新 (legal コア、次 account)

## 学習・改善
- decouple パターンを機能 (feature) にも適用開始。機能の UI 非依存コア = バリデーション / 状態判定 / レコード構築。`src/features/<name>/` 配置
