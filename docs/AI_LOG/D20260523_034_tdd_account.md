# D20260523_034 — /flow:tdd account (UI 非依存コア)

```yaml
session_id: D20260523_034_tdd_account
command: /flow:tdd
mode: feature (UI 非依存コアのみ、React section/modal/hook は defer)
target: account
started_at: 2026-05-23T18:06:00+09:00
last_updated: 2026-05-23T18:07:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_010_feature_account, D20260523_033_tdd_legal]
dispatched_by: /flow:auto (continuous loop iteration 6)
```

---

## Step 1-4: スコープ + 軽重判定

`account` は設定画面 + アカウント削除 + OAuth リンク UI。UI 非依存のドメインロジックのみ実装:

### 実装 (今回、純ロジック + DI)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `errors.ts` | AccountError / AlreadyDeletedError / NotPendingDeletionError | |
| `settings.ts` | validateLocationPrecision + deriveAiConsentChange (toggle→revokedAt/consentLog) | UT-AC-L01〜L03, AI02/AI03, P01 |
| `deletion.ts` | sanitizeDeletionReason (trim 500) + isPurgeEligible (grace 30日 境界) + requestAccountDeletion/cancelAccountDeletion (state 遷移、DI) | UT-AC-A02/A03/A05, D03, PG01〜PG03, B01 |
| `index.ts` | barrel | |

### Defer (app bootstrap、React 必要)
- `useUserSettings` hook + optimistic update/rollback (UT-AC-H01〜H04)
- React sections/modals (AccountSection/DeleteAccountModal/DeletionPendingGate/LocationPrecisionSection/AiConsentSection/PrivacySection) (UT-AC-S/D/G/L UI 部/AI UI 部/P UI 部)
- Sentry reconfigure 呼出 (UT-AC-P01 副作用)
- 実 RPC (request/cancel) + purge-deleted-users Edge Function の Storage/DB 削除 (UT-AC-PG04/PG05、RLS UT-AC-E01)

---

## decisions

### D20260523-099 — Step 1 スコープ (account UI decouple)

- **chosen_type**: auto-recommended (decouple 方針継続)
- **chosen**: settings/deletion/errors コアを実装、React UI/hook/Sentry/RPC/purge glue は app bootstrap へ defer
- **context**: account は設定 UI + 削除フロー。テスト可能なドメインロジック (location 検証 / AI consent toggle 導出 / 削除理由 sanitize / purge 適格判定 / 削除 state 遷移) を切り出し

### D20260523-100 — Step 6 全テスト結果

- **chosen_type**: auto-recommended
- **chosen**: 16 tests pass、全体 300/300 pass、typecheck clean
- **context**: account 行 98.59% / 分岐 95.65% (settings/deletion/errors 100%、削除 critical path 100%)

---

## 生成・更新アーティファクト
- 実コード: `src/features/account/{errors,settings,deletion,index}.ts` (4 新規) + test (2 新規、16 tests)
- レポート: `101_account_IMPL_REPORT.md` / `102_account_UNIT_TEST_REPORT.md`
- INDEX: `account/INDEX.md` / `docs/INDEX.md` → コア実装完了
- SCENARIO §5: Phase 3 進行更新 (account コア、次 capture)

## 学習・改善
- 削除 grace 判定を純関数 isPurgeEligible(deletedAt, now, graceDays) 化 → fake timers 不要で 30日境界を deterministic 検証
