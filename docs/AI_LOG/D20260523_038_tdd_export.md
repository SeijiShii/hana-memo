# D20260523_038 — /flow:tdd export (UI 非依存コア)

```yaml
session_id: D20260523_038_tdd_export
command: /flow:tdd
mode: feature (UI 非依存コアのみ、PDF 生成 / 画像 ZIP / React UI は defer)
target: export
started_at: 2026-05-23T18:18:00+09:00
last_updated: 2026-05-23T18:19:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_014_feature_export, D20260523_037_tdd_billing]
dispatched_by: /flow:auto (continuous loop iteration 10)
備考: D20260523-108 全テスト 22 pass、全体 365/365、export 行 98.46% / 分岐 96.15%。CSV BOM デフォルト ON (E-EX-005)
```

---

## Step 1-4: スコープ

`export` は PDF 図鑑 (PWYW unlock) + CSV/JSON/画像 ZIP。UI 非依存のデータ整形・検証を実装:

### 実装 (今回、純関数)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `errors.ts` | ExportError | E-EX 系 |
| `csv.ts` | toCsv (UTF-8 BOM + エスケープ) + DISCOVERY_CSV_COLUMNS | E-EX-005 |
| `validation.ts` | validateMonthRange / validatePdfCount (0→E-EX-003, >200→E-EX-001) / requirePdfUnlocked (E-EX-004) | §4.1 |
| `filename.ts` | pdfFilename / csvZipFilename / imagesZipFilename | UC1-4 |
| `index.ts` | barrel | |

### Defer (app bootstrap、jsPDF/canvas/React 必要)
- PDF 生成 (html2canvas + jsPDF、UC1)、画像 ZIP fetch + 並列制御 (UC4)
- React UI (フィルタ / ダウンロードボタン / unlock 誘導)、実 DB SELECT + Storage download

---

## decisions

### D20260523-107 — Step 1 スコープ (export UI decouple)

- **chosen_type**: auto-recommended (decouple 方針継続)
- **chosen**: csv/validation/filename/errors コアを実装、PDF/画像 ZIP/React UI/IO glue は app bootstrap へ defer
- **context**: export は出力整形。テスト可能なコア (CSV 生成 + BOM + エスケープ / 期間・件数・unlock 検証 / ファイル名規約) を切り出し
