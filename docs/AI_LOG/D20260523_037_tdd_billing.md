# D20260523_037 — /flow:tdd billing (UI 非依存コア)

```yaml
session_id: D20260523_037_tdd_billing
command: /flow:tdd
mode: feature (UI 非依存コアのみ、Stripe SDK / Vercel handler / React UI は defer)
target: billing
started_at: 2026-05-23T18:15:00+09:00
last_updated: 2026-05-23T18:16:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_013_feature_billing, D20260523_036_tdd_notebook]
dispatched_by: /flow:auto (continuous loop iteration 9)
備考: D20260523-106 全テスト 19 pass、全体 349/349、billing 行 99.21% / 分岐 97.77% (webhook べき等性 WH03 100%)。OAuth 必須は auth LinkRequiredError 再利用
```

---

## Step 1-4: スコープ

`billing` は AI クレジット購入 + PDF PWYW アンロック + Stripe webhook + 収益 CSV。最も domain ロジックが厚い。UI 非依存コアを実装:

### 実装 (今回、純関数 + DI)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `errors.ts` | BillingError / InvalidAmountError (OAuth 必須は auth LinkRequiredError 再利用) | E-BL-002 |
| `pricing.ts` | validateQuantity (1-10) / validatePwywAmount (100-10000) / aiCreditsAmountJpy / aiCreditsGranted (20*qty) / requireLinked | UT-BL-CS01〜CS06 |
| `webhook.ts` | mapStripeEvent (checkout.session.completed→unlock) + applyBillingWebhook (idempotent: webhook_dedupe + sessionId、BillingStore DI) | UT-BL-WH01/WH02/WH03/WH05/WH06 ([SEC-006]) |
| `revenue.ts` | grossMargin + buildRevenueCsv (列順) | UT-BL-ER03/ER05 |
| `index.ts` | barrel | |

### Defer (app bootstrap、Stripe SDK/React 必要)
- `create-checkout-session` / `stripe-webhook` Vercel Function (Stripe SDK + 署名検証、UT-BL-CS07/WH04/WH07)
- `useAiCredits`/`usePdfUnlocked` hook、checkoutApi、successConfirm poll、OAuthRequiredModal (UT-BL-H/A/SC/OM)
- `export-revenue` の実 Storage 保存 + Slack 送信 (UT-BL-ER01/ER02/ER06)

---

## decisions

### D20260523-105 — Step 1 スコープ (billing UI decouple)

- **chosen_type**: auto-recommended (decouple 方針継続)
- **chosen**: pricing/webhook/revenue/errors コアを実装、Stripe SDK/Vercel handler/React UI glue は app bootstrap へ defer
- **context**: billing は課金ドメイン。テスト可能なコア (価格・数量検証 / クレジット計算 / Stripe event mapping + べき等性 / 収益集計) を切り出し、OAuth 必須は auth LinkRequiredError 再利用
