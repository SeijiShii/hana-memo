# D20260523_035 — /flow:tdd capture (UI 非依存コア)

```yaml
session_id: D20260523_035_tdd_capture
command: /flow:tdd
mode: feature (UI 非依存コアのみ、React hook / camera / Realtime は defer)
target: capture
started_at: 2026-05-23T18:09:00+09:00
last_updated: 2026-05-23T18:10:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_011_feature_capture, D20260523_034_tdd_account]
備考: D20260523-102 全テスト 11 pass、全体 309/309、capture 行 98.46% / 分岐 94.73% (errors/flow/note/status 100%)。再利用: ai QuotaExceededError + storage UploadFailedError + account isAiConsentActive
dispatched_by: /flow:auto (continuous loop iteration 7)
```

---

## Step 1-4: スコープ + 軽重判定

`capture` は撮影→AI識別→保存の中核フロー。UI 非依存のオーケストレーション + 状態遷移を実装:

### 実装 (今回、純ロジック + DI)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `errors.ts` | CaptureError / AiConsentRequiredError | |
| `note.ts` | sanitizeUserNote (trim 200) | UT-CA-E03 |
| `status.ts` | DISCOVERY_STATUSES + isTerminalStatus + canRetry + nextStatusOnRetry | UT-CA-R01/R02 (status 遷移) |
| `flow.ts` | runCapturePipeline (consent/quota pre-check → createDiscovery → upload[fail→delete] → attach → identify、CaptureDeps DI) | UT-CA-CF01/CF02/CF03 |
| `index.ts` | barrel | |

- quota は `_shared/ai QuotaExceededError`、AI 同意は `account isAiConsentActive` を再利用。

### Defer (app bootstrap、React/canvas/Realtime 必要)
- `useImageConvert` (canvas WebP 変換 + EXIF strip、UT-CA-IC01〜IC06 — helpers/image.ts は browser-only で実装済、hook は app)
- `useGeolocation` (UT-CA-G01〜G05、helpers/location roundLocation 再利用)
- `useCaptureFlow` React hook + `useIdentifyStatus` (Realtime/poll、UT-CA-IS/CF04〜CF07 の UI 部)
- 実 DB INSERT/UPDATE/DELETE (captureApi、UT-CA-A01〜A04) + camera UI (UT-CA-E01)

---

## decisions

### D20260523-101 — Step 1 スコープ (capture UI decouple)

- **chosen_type**: auto-recommended (decouple 方針継続)
- **chosen**: note/status/flow/errors コアを実装、React hook/canvas/Realtime/DB glue は app bootstrap へ defer
- **context**: capture は撮影フロー。テスト可能なコア (補助メモ整形 / discovery status 遷移 / capture pipeline orchestration) を切り出し、quota/consent は ai/account を再利用
