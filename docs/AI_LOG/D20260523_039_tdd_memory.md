# D20260523_039 — /flow:tdd memory (UI 非依存コア、Phase 3 最後の対象)

```yaml
session_id: D20260523_039_tdd_memory
command: /flow:tdd
mode: feature (UI 非依存コアのみ、バッジ/カルーセル React UI / localStorage / 実 DB は defer)
target: memory
started_at: 2026-05-23T18:21:00+09:00
last_updated: 2026-05-23T18:22:00+09:00
状態: 完了 (UI 非依存コア)
完了ステップ一覧: [Step 1-4 判定, Step 5 実装, Step 6 全テスト, Step 7 レポート, Step 9 INDEX, Step 10 整合性, Step Z commit]
依存セッション: [D20260522_015_feature_memory, D20260523_038_tdd_export]
dispatched_by: /flow:auto (continuous loop iteration 11、Phase 3 最終対象)
備考: D20260523-110 全テスト 9 pass、全体 373/373、memory 行 97.29% / 分岐 88.88%。**Phase 3 コア実装 14/14 完遂** (横断 7 + 機能 7、累計新規テスト 254 / 全体 373 green)。次フェーズ = app/api bootstrap (defer glue 一括 wiring + SDK install)
```

---

## Step 1-4: スコープ

`memory` は「去年の今頃に見た花」レコメンド (UC1/UC2)。UI 非依存のレコメンドロジックを実装:

### 実装 (今回、純関数)
| ファイル | 責務 | 対応テスト |
|---|---|---|
| `recommend.ts` | lastYearWindow (前年 ±15 日) + selectLastYearMemories (identified + 期間 + 最大 5) + hasMemories + memoryCacheKey (日次) | UC1/UC2, E-ME-002/003 |
| `index.ts` | barrel | |

- 日付計算は `_shared/helpers/date` (addDays/parseISO/formatDate、実装済) を再利用。

### Defer (app bootstrap、React/localStorage 必要)
- 「去年の今頃」バッジ + 横スクロールカルーセル React UI (notebook TimelineView 統合)
- localStorage 当日キャッシュ (TTL 24h)、実 DB SELECT (前年 ±15 日 fetch、E-ME-001 silent fail)

---

## decisions

### D20260523-109 — Step 1 スコープ (memory UI decouple、Phase 3 最終)

- **chosen_type**: auto-recommended (decouple 方針継続)
- **chosen**: recommend コアを実装、バッジ/カルーセル React UI / localStorage / 実 DB glue は app bootstrap へ defer
- **context**: memory は季節レコメンド。テスト可能なコア (前年同期間ウィンドウ計算 / メモリ選定 / 日次キャッシュキー) を切り出し、日付計算は helpers/date 再利用。本対象完了で Phase 3 コア実装 14/14 完遂
