# billing E2E テスト計画 (数量入力撤去)

> **入力**: `./001_REVISE_SPEC.md`, `e2e/billing.spec.ts` (既存 no-key billing ジャーニー)
> **最終更新**: 2026-05-27

---

## 1. 変更 UC シナリオ
| シナリオ ID | 前提 | 操作ステップ | 期待結果 |
|---|---|---|---|
| E2E-R2-01 | /billing 表示 (no-key) | 画面を確認 | 数量入力 (`aria-label="数量"`) が**存在しない** (count 0)。「¥100 で購入する」ボタンが見える |
| E2E-R2-02 | /billing 表示 | 「¥100 で購入する」押下 (Checkout は seam/モック) | quantity=1 で checkout 発行導線に入る (実 Stripe は叩かない) |

## 2. リグレッションシナリオ (既存 billing.spec.ts、重要度高)
| UC | シナリオ ID | 確認観点 |
|---|---|---|
| 無課金導線 | 既存 E2E (購入ボタン表示) | 「購入する」ボタンが連携ゲートに隠されず表示 (revise_001 ガード維持) |
| 廃止機能ガード | 既存 E2E (PWYW/PDF/export 不在) | 数量入力も「残っていない UI」リストに追加 (回帰ガード) |
| 連携モーダル非表示 | 既存 E2E-R-01 | billing パスで「アカウントを連携する」非表示 (維持) |

## 3. 移行検証シナリオ
(マイグレーションなし → 該当なし)

## 4. 環境要件差分
なし (既存 no-key Playwright スモークに E2E-R2-01 を追加するだけ)。

## 5. 期待 KPI
| 指標 | 目標 |
|---|---|
| billing E2E | 全 green (数量入力非表示の回帰ガード含む) |

## 6. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-05-27 | 初版作成 | /flow:revise |
