# クレーム調査レポート

**claim id**: 001
**実施日**: 2026-05-25
**対象機能**: _shared/ai (AI 同定 quota)
**緊急度推定**: high

## 1. クレーム原文

```
（実機検証中、新規ゲストで）1回画像ファイルを選んだだけでこうなった
→ モーダル「今月の識別回数を使い切りました / AI 識別の残り回数がありません。
   クレジットを追加すると引き続き識別できます。」(課金画面へ / あとで)
（スクリーンショット添付）
```

## 2. 分解結果

### 2.1 期待挙動 (Expected)
起動直後の新規匿名(ゲスト)ユーザーが、画像を選ぶと **AI が植物名を返す** (無料枠/お試しで識別できる)。

### 2.2 現実挙動 (Actual)
画像を 1 回選んだ時点で即「今月の識別回数を使い切りました (残り 0)」モーダルが出て、AI 同定が一度も実行されない。

### 2.3 発生条件
- 操作: アプリ起動 (匿名サインイン成立済) → 撮影で画像ファイルを 1 回選択
- 環境: localhost:3000 (vercel dev)、Windows ブラウザ、実 Clerk test + 実 Neon (drizzle push 直後)
- ユーザー属性: 新規匿名ユーザー (trial_used_count=0, ai_credits_remaining=0)

### 2.4 影響範囲
- 該当ユーザー: **全新規ユーザー** (匿名スタートする全員)。無料枠の価値提供が 0 回も成立しない
- 業務影響: コア体験 (撮るだけで名前が分かる) が新規ユーザーに一切届かない = α の致命
- データ影響: なし

### 2.5 報告経路
- 経路: 開発者自身の実機検証 (release Phase 2 / runtime verify)
- 温度感: 冷静 (検証フィードバック)

### 2.6 報告者文脈
新規ユーザーが「撮るだけで名前が分かる」を無料で体験したいのに、初回からクレジット切れ扱いで弾かれる。

## 3. 過去類似クレーム
該当なし (claim 初回)。直前の auth revise_001 (匿名サインイン実装) で session 成立 → 次段の quota ゲートで露見した downstream イシュー。
