# _shared/db 単体テスト計画（drizzle-orm SQL インジェクション CVE 対応）

> **入力**: `./001_REVISE_SPEC.md`, `./002_REVISE_PLAN.md`, 既存 `src/shared/db/*.test.ts` (schema/access/errors、28 tests)
> **最終更新**: 2026-05-24

---

## 1. 追加テストケース

> 本改修は依存アップグレードであり、振る舞いは不変。新規テストは最小限 (回帰補強のみ)。

### 1.1 正常系
| ID | 対象 | 入力 | 期待出力 |
|---|---|---|---|
| U-01 | `schema.ts` の各テーブル `$inferSelect` 型 | 型推論 | 0.45 でも既存と同一の型形状 (型レベル、tsc で担保) |

### 1.2 異常系
| ID | 対象 | 失敗条件 | 期待振る舞い |
|---|---|---|---|
| U-02 | identifier escaping 回帰 (任意) | 動的識別子を含むクエリビルド | 0.45.2 で適切にエスケープ (CVE 修正の確認、ユニットで再現できる範囲) |

### 1.3 境界値
| ID | 対象 | 境界 | 期待振る舞い |
|---|---|---|---|
| — | (なし) | — | — |

## 2. 修正テストケース

| ID | 対象 | 修正前 | 修正後 | 理由 |
|---|---|---|---|---|
| (条件付き) | drizzle API 直叩きテスト | 0.36 想定 | 0.45 で型/シグネチャ変更があれば追従 | 型エラー発生時のみ。発生しなければ修正不要 |

## 3. 削除テストケース

| ID | 対象 | 削除理由 |
|---|---|---|
| (なし) | — | — |

## 4. リグレッション強化

- **既存 28 テスト全維持** (schema 定義 / `withUserScope` / `assertOwner` / errors)。これが本改修の中核 GREEN ゲート
- 全 PJ 373 テストの green 維持を CI/ローカルで確認 (drizzle は全 feature の基盤のため波及確認が最重要)
- `access.test.ts` の [SEC-005] 認可ネガティブテストは drizzle バージョン非依存だが、アップグレード後も green を再確認

## 5. Mock 方針差分

| 対象 | 前回 | 今回 | 理由 |
|---|---|---|---|
| DB 接続 | mock / dev branch | 変更なし | アップグレードで mock 方針は不変 |

## 6. カバレッジ目標

| 種別 | 目標 | 根拠 |
|---|---|---|
| 行 | 80% | 既存継承 (vitest.config.ts) |
| 分岐 | 70% | 既存継承 |

> アップグレードで新規ロジックはほぼ増えないため、既存カバレッジ維持が目標。

## 7. 更新履歴
| 日付 | 変更概要 | 実行者 |
|---|---|---|
| 2026-05-24 | 初版作成 | /flow:revise (D20260524_044) |
